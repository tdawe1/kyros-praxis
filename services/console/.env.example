NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=changeme

# Optional API endpoints used by client code
# If used, expose as NEXT_PUBLIC_* in Next.js for browser access
# API base HTTP URL for REST calls
API_URL=http://localhost:8000
# WebSocket endpoint (if your app consumes WS directly)
WS_URL=ws://localhost:8000
